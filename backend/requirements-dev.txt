# Test + lint dependencies. Install with:
#   pip install -r requirements-dev.txt
#
# Keeps the production image (requirements.txt) lean — none of these ship in
# the runtime container. CI / contributors install both files.

-r requirements.txt

pytest==8.4.2
pytest-asyncio==1.2.0
pytest-cov==7.0.0
pytest-mock==3.15.1
pytest-xdist==3.8.0
httpx==0.28.1
anyio==4.11.0
coverage[toml]==7.10.7
